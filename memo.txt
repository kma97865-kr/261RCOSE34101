운영체제 cpu shceduler 진행 스케쥴
함수 이름은 헤더네임_기능()

5/16 : proess.h, process.c(프로세스) queue.h, queue.c(큐)
이슈 : process 해제 책임을 어디서 질 것인가? process? queue?
다음 task : init_config() 초기화 모듈, final_config() 마무리 모듈

5/18 : config.h, config.c(각 알고리즘을 실행하기 전 초기화, 끝난 후 청소)
다음 task : 기본 알고리즘 == FCFS, 

5/20 : job_queue.h, job_queue.c 완성
다음 task : running_process.h, running_process.c

5/21 : running_process.h, running_process.c 완성
다음 task : FCFS 
이슈: ready_queue에서 running_process로 넘어가는 과정을 함수, check_ready_put_running(int)

time = -1
while (모든 큐가 empty){
    time ++
    job_queue에서 넘어올 애들 넘어옴 -> job_queue.h : 들어온 애들을 arrival time 대로 정렬, time 보고 넘겨줌
                                    ->NULL 튀어나올 때까지 돌림
        있으면 ready_queue에 push
    running 검사(preemption x) -> running_process.h : running 하고 있는 애를 time 보고 종료, burst time을 깎는 방향
    running 검사에서 빼는 과정과 넣는 과정을 분리
        process가 실행 중이면 continue;
        process가 종료되거나 비어있으면
            ready queue에서 pop하고 running process로 등록
}

5/22 : evaluate.h, evaluate.c, gantt chart
terminate_queue -> turnaround time(끝난 시간 - 도착 시간), waiting time(대기 큐에 있는 시간), Gantt Chart
기본 구상 : trounaround time -> 1 * N 동적 배열 = 마지막 끝난 시간 저장 -> 도착 시간 빼서 구함
            waiting time -> sigma(시작 시간) - sigma(종료 시간) + 마지막 종료 시간 - 도착 시간
이슈 : 프로세스를 생성한 다음, 여러 알고리즘에서 돌려보고 싶은데, 현재는 돌리면 모두 terminate_queue로 넘어가고, 분할되서 변형될 예정

    : non_preemptive SJF
        priority_queue.h, priority_queue.c
        target: ready queue
        priority -> priority (SJF : priority = burst time) 
이슈 : (non_preemptive 한정) push -> priority_push로 바꾸면 될 거 같은데 코드 재활용?

    : preemptive priority, preemptive sjf
        preemption이 될 경우 판단
            필요 없을 경우, 똑같이 작동
            필요한 경우 -> 프로세스를 잘라서, terminate queue에 넣고, 나머지를 ready_queue에 넣고, 쓰던 애는 해제
    : Round Robin

다음 task: I/O interrupt -> wait queue.h, wait queue.c가 필요함
                        -> running_process -> wait_queue, wait_queue->ready_queue
                        -> I/O 실행을 running 하면서 random 으로 적용하는 방법
                        -> I/O end time은 상관없음(현재 start_time 보다 앞서기만 하면 됨)

5/23 : I/O interrupt
    I/O interrupt 발생은 매 시간 랜덤 -> probability로 조절
    wait_queue_push -> wait queue는 io_end_time 대로 정렬해서 사용
    wait_queue->ready_queue 는 job_queue->ready_queue와 같은 priority 사용
        NULL : FCFS (wherever process come from wait queue or ready queue), do not use arrival time
        priority : priority
        SJF : reamining cpu burst time as priority
이슈: i/o 발생 on/off 를 main 함수 or 사용자 입력으로 받아서 관리할 수 있게
다음 task : 추가적인 스케쥴링 함수
다다음 task : wrapping
다다다음 task : refactoring

6/3 : 추가적인 스케쥴링 함수 : UNIX(dynamic priority)
    priority = base + recent CPU usage + nice
    aging - penalty -> I/O 프로세스의 응답성에 장점이 있지만 실제로 확인하기에는...?
    후보 1. future - knowledge scheduling : SJF 에서 현재 실행시간 보다 짧은 애를 대기하고 넣는게 가능할 경우 넣는 방법
    후보 2. UNIX : recalculate once per second, priority = (recent CPU usage /2) + basePriority + Nice(user implement)
                    recent CPU usage = how often a process has used the CPU since priorities were last recalculated
    후보 3. ensures (a CPU usage / # of process)
    다음 task : wrapping

6/4 : wrapping
    queue_copy(source queue, dest queue){
        if(source queue == NULL || dest queue == NULL) return;
        Process temp_p = queue_get_data(source)
        while(temp != NULL){
            //create new process
            queue_push(dest queue, new process) //push to queue
        }
    }

6/5 : I/O를 input으로 switch, parameters, evaluate 함수 최적화

6/6 

